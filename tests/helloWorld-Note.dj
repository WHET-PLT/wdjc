song score () 
{
	/*simple note test */

	double pitchA; 
	double volume; 
	double duration; 

	pitchA = 440; 
	volume=100; 
	duration=4; 
	
	note n = note (pitchA, duration, volume);
	chord c = chord(n);
	track t = track( 0 );
	score s = score( t );
	return s; 
}