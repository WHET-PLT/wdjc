Song note(){
	/*simple note test */

	int pitchA; 
	int volume; 
	int duration; 

	pitchA = 440; 
	volume=2; 
	duration=4; 

	n = note (pitchA, volume, duration); 
	chord c = chord(n);
	track t = track(c);
	score s = score(s);
	return s; 
}