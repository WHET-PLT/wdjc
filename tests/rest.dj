song score ()
{
	/* rest of duration 5  */
	/* make a chord which is a rest + a note */	
	rest r;
	chord d;
	double i = 3;
	double a;
	double b;
	double c;

	
	r = rest (5);
	r = rest (i);
	

	note n = note (a, b, c);
	/* TODO chord should be able to take rests too */
	d = n : n;

	return 1;


} 