song int ()
{
	/* rest of duration 5  */
	/* make a chord which is a rest + a note */	
	rest r;
	chord c;
	int i = 3;

	r = rest (5);
	r = rest (i);

	note n = note (a, b, c);
	c = n : r;

	return 1;


} 