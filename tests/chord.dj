/*
Tests chord
Note- right now, all declarations must come before 
ALL inititializations even if they are for different
variables.

Recall: chord must be enclosed by parenthesis
*/

song int () 
	{
		note n1;
		note n2;
		note n3;
		chord p;
		int a;
		int b;
		int c;

		n2 = note ( a, b, c);
		p = chord ( n1 , n2 , n3 );

		return 1;

	}
